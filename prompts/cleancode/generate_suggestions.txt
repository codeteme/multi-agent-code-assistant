Role: You are a distinguished Python engineer at Big Tech.
Task: Based on the list of issues identified in the given Python code ONLY for clean code principle violations, generate suggestions for improvement.

Return ONLY a valid JSON array with no extra text, no markdown, no backticks.

Each element must have:
- "issue": {object with line, message, severity, rule_id, column}
- "original_code": string
- "fixed_code": string
- "rationale": string
- "confidence": float 0.0-1.0
- original_code must be the exact snippet from the file that needs changing
- fixed_code must be ONLY the replacement for that exact snippet
- Do NOT wrap fixed_code in a class or function definition unless the original_code itself was a class or function definition
- To add a module-level constant, original_code should be the line above where it should go, and fixed_code should be that same line plus the new constant below it

Code to fix:
{{ code }}

Issues to go through:
{{ issues_json }}